# Notes — 004-004

## Interpretation

Create the complete Days One word as one 3D text object, rotate it 45 degrees counter-clockwise, bake that rotation into the source outline, convert it to a mesh, and fit the resulting mesh into the guide envelope. Reduce text tessellation so the eventual fitted mesh better respects the provisional triangle target.

## Observations

Blender was invoked once, but generation stopped before saving or rendering. Blender 5.2 does not allow a live text object's rotation transform to be applied directly; only scale can be applied at that stage. Consequently, no `logo-004-004.blend` or `logo-004-004.png` was generated.

The requested construction remains possible by preserving the 45-degree rotation on the live text, converting it to a mesh, and then applying the rotation transform to the converted mesh before guide fitting. That corrected order will be used in the next experiment.

## Comparison

There is no preview from this failed run, so no visual comparison with `logo-004-003.png` is possible.
